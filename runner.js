;(function () {
  'use strict'

  // pm2 has been removed as a project dependency.
  // Use `node app.js` directly, or install pm2 globally: npm install -g pm2 && pm2 start app.js --name trudesk
  require('./app.js')
})()
